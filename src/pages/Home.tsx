import MenuBar from '../components/Home/MenuBar';
import theme from '../styles/theme';

const Home = () => {
  return (
    <div>
      <MenuBar theme={theme} />
    </div>
  );
};

export default Home;
